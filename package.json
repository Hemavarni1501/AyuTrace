{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
  "dev": "vite --mode dev",
  "build": "vite build",
  "lint": "echo 'linted'",
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}

}
